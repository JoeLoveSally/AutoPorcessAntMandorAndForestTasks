# 开发与验证记录

更新时间：2026-08-31

## 自动化检查

- `pytest`：113 项通过。
- `ruff check src tests`：通过。
- `compileall`：通过。
- `git diff --check`：通过。

## 设备与时延

测试设备：V2227A，1440×3200，ADB 序列号由本地 `config/config.toml` 配置。

| ADB 操作 | P50 | P95 | 结论 |
| --- | ---: | ---: | --- |
| 前台 Activity | 146 ms | 148 ms | 适合安全前置检查 |
| PNG 截图 | 2245 ms | 2437 ms | 适合普通页面，不适合能量雨 |
| UI 树 | 2555 ms | 2666 ms | 与截图组成普通页面完整观测 |

因此普通任务使用“截图 + UI 树 + 页面后置条件”，能量雨使用 Android `screenrecord` 原始 H.264 流与持续 ADB Shell 触控。滑动广告在首次确认页面后采用三次一组的已验证滑动，组间重新观测，避免每秒动作被完整观测的 2 秒以上延迟打断。

实时通道读取 5 帧用时 4.607 秒、读取 30 帧用时 4.799 秒；首帧包含 Android 编码器和 FFmpeg 解码器启动时间，后续帧以实时速率连续到达。scrcpy v4.1 官方包下载和校验通过，但其未封口 MKV 录制文件在本机组合下不能稳定被 FFmpeg 实时消费，所以仅保留为诊断依赖，不作为能量雨主通道。

## 已完成真机校准

- 支付宝首页、庄园首页、家庭签到和动态排序的家庭任务列表。
- 固定选择首个公益项目、默认 1 颗蛋、捐蛋成功页、三次返回及 `(1/1)` 验证。
- 请客美食确认与 `(1/3)` / 时间限制状态验证。
- 喂食确认与 `(1/1)` 验证。
- 庄园日记入口与日记页模板；当按钮点击后未变化时单次运行记录为不可执行，不重复点击。
- 饲料签到、无 A/B 前缀的两选一题目及答题结果页。
- 修复 WebView 保留隐藏 DOM 时把不可见确认按钮误当成当前弹窗的问题。
- 森林主页、三颗普通能量、延迟出现的能量签到气泡、奖励弹窗及下滑后“立即领取”按钮。
- 森林“找能量”Canvas 控件、森林广场的能量雨/真爱合种/合种入口。
- 原生 H.264 实时帧通道：5 帧和 30 帧读取均成功。
- 点击恢复只在动作尚未发出时启用，并先恢复到动作源页面；动作坐标已经发送时禁止自动重试。
- 芭芭农场入口弹窗、任务页、主施肥按钮、挑战弹窗及两种丰收礼包关闭行为；完成奖励保留在任务页，不自动领取。
- 小鸡厨房为整屏 Canvas：已校准今日食材、树上食材、爱心食材店、`领10g食材`、两次做美食及菜谱关闭；厨房任务奖励保留在饲料任务页，不自动领取。

## 2026-08-31 代码级稳定性加固

- 关键点击统一验证观测年龄、页面稳定性、Activity、源元素、目标页面及完成标记；页面切换期的单帧结果不会直接判定成功。
- 后置条件可声明必须出现、至少出现一个和必须消失的元素；生产 Workflow 的普通点击均已声明目标页面。
- 恢复从“只比较页面类型”升级为校验稳定性、遮罩、Activity 和任务元素，并为庄园饲料任务及抽奖增加明确重进路径。
- 外部浏览返回时同时核对 Activity，避免支付宝 WebView 保留的旧 DOM 被误判为已经返回任务列表。
- 根据既有真机截图确认：庄园日记入口坐标有效，失败根因是首次进入的三步引导遮罩。检测器现将该画面识别为日记页并安全跳过引导。
- 捐蛋、请客、答题、做美食、浇水、抽奖和能量雨在不可逆动作前写入 `IN_PROGRESS`，完成验证后写入终态；启动日志会列出跨运行仍未闭合的步骤。
- 能量雨按配置间隔及实际点击保存标注帧与 JSONL 元数据，写盘使用有界异步队列，诊断速度不足时丢样本而不拖慢实时点击。

以上项目本轮只完成离线代码检查与测试，没有操作手机，因此不视为真机通过。

## 待最终真机回归

最后一次已确认的真机停点是森林好友树页面：当前好友没有“一键收”，只有礼物气泡；按既定规则应直接进入下一位好友。手机此刻实际页面需在下一次回归开始前重新观察，不能沿用该历史停点直接点击。后续回归需执行：

1. `./scripts/ants-auto doctor`
2. `./scripts/ants-auto forest`
3. 次日执行一次 `./scripts/ants-auto daily`，覆盖未完成状态下的庄园全流程。

森林完整回归仍需覆盖好友一键收/森林寻宝、左右抽奖、两种合种浇水以及两轮能量雨。最终重点记录能量雨两轮的帧间隔、唯一目标数、点击数、确认命中数与命中率；验收线为 80%。
